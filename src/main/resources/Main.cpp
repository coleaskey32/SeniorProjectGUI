#include <iostream>
#include <string>

int main() {
    // Sample data to be sent
    std::string data = "Hello from C++!";

    // Writing data to stdout
    std::cout << data << std::endl;

    return 0;
}